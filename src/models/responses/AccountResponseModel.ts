export interface getAccountResponse {
  email: string;
  name: string;
  branch: string;
  password: string;
}
export interface addAccountResponse {
  email: string;
  name: string;
  branch: string;
  password: string;
}
